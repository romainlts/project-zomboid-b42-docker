# secrets & local config / FR : secrets & config locale
.env
*.env
!.env.example

# runtime data / FR : donnees runtime
panel/data/
panel/logs/
data/
backups/
*.log

# AI assistants (local context, not versioned)
# FR : assistants IA (contexte local, non versionne)
CLAUDE.md
.claude/

# local documentation (setup notes, git identity, local paths)
# FR : documentation locale (notes de mise en place, chemins locaux)
docs/

# OS / editors / FR : OS / editeurs
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/

# misc / FR : divers
*.tmp
*.bak
node_modules/
